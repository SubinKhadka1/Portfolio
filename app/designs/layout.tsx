import DesignsPageShell from "@/components/DesignsPageShell";

export default function DesignsLayout({ children }: { children: React.ReactNode }) {
  return <DesignsPageShell>{children}</DesignsPageShell>;
}
